@using sevco5Beta.ViewModels
@model DominiosListViewModel






<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">Dominios <b class="caret"></b></a>
    <ul class="dropdown-menu">
       @foreach (@sevco5Beta.Models.dominios item in @Model.DominiosList)
        {
            <li>@Html.ActionLink(item.Nombre_dominio, "ProcesosList", "Procesos", new { id = item.Id_dominio }, null)</li>

           

        }

    </ul>

</li>